ServerModule:
  Name:               A16Session
  Target Platform:    Windows/IBM i/Linux

  Multilingual Details:
  - ENG:
      Description:        "Application Session"

  - FRA:
      Description:        "Application Session"

  Source: |
    Begin_Com Role(*EXTENDS #PRIM_SRVM) Sessionidentifier('A16SESSION')
    
    Define Field(#A16UserName) Type(*STRING)
    Define Field(#A16Password) Type(*STRING)
    Define Field(#A16ReturnCode) Type(*CHAR) Length(2)
    
    Srvroutine Name(SignIn)
    Field_Map For(*INPUT) Field(#A16UserName) Parameter_Name(UserName)
    Field_Map For(*INPUT) Field(#A16Password) Parameter_Name(Password)
    Field_Map For(*OUTPUT) Field(#A16ReturnCode) Parameter_Name(ReturnCode)
    
    * TODO: Check username and password
    
    #COM_OWNER.StartSession Timeout(3600)
    
    #A16ReturnCode := OK
    
    Endroutine
    
    Srvroutine Name(SignOut) Session(*REQUIRED)
    
    #COM_OWNER.EndSession
    
    Endroutine
    
    End_Com
